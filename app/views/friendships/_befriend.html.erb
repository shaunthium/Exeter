<%= form_for(current_logged_in_user.friendships.build) do |f| %>
    <div><%= hidden_field_tag :friend_id, @user.id %></div>
    <%= f.submit "Befriend", class: "btn btn-primary", id: "submit-btn" %>
<% end %>
