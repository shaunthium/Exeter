<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-brand">
        <%= link_to "Exeter", root_path %>
    </div>
    <% if logged_in? %>
      <div>
        <ul class="nav navbar-nav navbar-right">
          <%= render 'shared/notifications' %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= current_logged_in_user.name %>
              <i class="caret"></i>
            </a>
            <ul class="dropdown-menu">
                <li>
                  <%= link_to "Edit your personal information", edit_user_path(current_logged_in_user.slug) %>
                </li>
                <li>
                  <%= link_to "Delete your account", user_path(current_logged_in_user.slug), method: :delete, data: { confirm: "Are you sure you want to delete your account?" } %>
                </li>
            </ul>
          </li>
          <li>
            <%= link_to "Logout", logout_path, method: "delete" %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>
